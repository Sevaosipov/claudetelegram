requests>=2.31
pdfplumber>=0.11
yfinance>=0.2
pandas>=2.0
lxml>=5.0
# Used directly by bafin.py and senate_efd.py. It was reaching the venv only as a
# transitive dependency of yfinance, which would break the moment yfinance dropped
# it -- and only at runtime, inside the BaFin pass.
beautifulsoup4>=4.12
# model_eval.py: logistic regression + gradient boosting for walk-forward
# evaluation of predictive skill. Pulls scipy, joblib, threadpoolctl.
scikit-learn>=1.6
