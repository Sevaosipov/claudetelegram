# Virtualenv (recreate with: uv venv --python 3.12 && uv pip install -r requirements.lock)
.venv/
__pycache__/
.pytest_cache/
*.pyc

# Secrets
.env

# Runtime data — machine-specific, regenerated by the bot / caches
data/*.db
data/*.db.bak-*
data/*.csv
data/*.log
data/universe_cache.json
data/cik_ticker_cache.json
data/*_seen*.json

# SDD scratch (git-ignored workspace for the executing skill)
.superpowers/
